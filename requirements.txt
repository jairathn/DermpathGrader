# pip fallback. pyproject.toml plus uv.lock are authoritative and are
# what Streamlit Community Cloud uses; keep this file in step with the
# [project.dependencies] list there.
#
#   uv sync                          exact pins from uv.lock
#   pip install -r requirements.txt  approximate

# 1.x is required, not preferred: structured outputs, adaptive thinking
# and the streaming helper the analyzers use do not exist on 0.x.
anthropic>=1.6.0
chromadb>=1.0.12
langchain-community>=0.3.24
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
numpy>=1.26
pillow>=11.2.1
pypdf>=5.6.0
streamlit>=1.45.1

# Managed hosts ship a SQLite older than the 3.35 chromadb requires and
# it cannot be apt-installed there. sqlite_compat.py swaps this in at
# import time; inert where the system SQLite is current.
pysqlite3-binary>=0.5.0; sys_platform == "linux"

# extract_tiles.py only, for reading .svs whole-slide images. Also needs
# the OpenSlide C library (apt install libopenslide0 / brew install
# openslide). Skip if tiles come from ImageScope or QuPath.
# openslide-python>=1.3.1
